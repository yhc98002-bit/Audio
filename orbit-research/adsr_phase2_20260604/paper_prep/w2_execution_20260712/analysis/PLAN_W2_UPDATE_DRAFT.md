# PLAN W2 Update Draft - Do Not Apply

`PLAN_UPDATE_DRAFT_STATUS = BLOCKED_ON_BOTH_PI_SIGNATURES`

This is a mechanical edit plan, not an active claim table. `paper_prep/PLAN.md`
must remain unchanged until the W2 amendment and corrected-instrument adoption
record both carry the required signatures.

## If Promotion Passes And Is Adopted

1. Add the corrected-instrument ID, thresholds, held-out counts, point metrics,
   one-sided lower bounds, and exact promotion artifact to the detector row.
2. Preserve each frozen apparent number and add calibrated rate plus 95% joint
   interval beside it; never replace history without the supersession link.
3. Update Stage 3, N2, spine prevalence, and Batch-3 selected-output rows only
   from `W2_PUBLICATION_RATES.csv` and its calibration audit.
4. Label Batch-3 outputs as retained/selection-conditioned and disclose the
   detector-dependent selection mechanism, duplicate-media roles, and
   instrument provenance.
5. Add corrected EVPD and bounded live-confirm results only if their independent
   guards and frozen pass criteria complete.

## If Promotion Fails

1. Leave every existing PLAN number and status unchanged.
2. Add only a limitation row: candidate Demucs/PANNs correction was evaluated
   but remained sensitivity-only under the predeclared promotion gate.
3. Do not use `corrected prevalence`, `validated detector`, or an unqualified
   undercount claim.

## Wording Constraints

- Allowed after adoption: `the frozen detector undercounted violations under a
  human-calibrated, held-out corrected instrument` with its scope and interval.
- Allowed direction statement: `consistent with a vocal-generation bias`.
- Forbidden: causal language, new hard difficulty bins, `impossible to retry`,
  generic-population wording for selected sets, and `proved no loss`.
