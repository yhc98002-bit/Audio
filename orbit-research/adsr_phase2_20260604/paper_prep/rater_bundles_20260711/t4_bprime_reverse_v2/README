t4_bprime_reverse_v2 blinded rating bundle (24 pairs).
Rate quality blind, reveal request context, then rate constraint and overall preference.
Important: open on a later day than t3; then export CSV and JSON.
