# Preserved Background Test Attempt

The first detached full-suite launcher exited without writing pytest output or
an exit artifact. Its zero-byte output file is preserved here. The final suite
is run in an attached PTY after all W2 reports are complete; only that attached
run is used for the reported test status.
