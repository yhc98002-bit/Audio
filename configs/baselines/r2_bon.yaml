project: audio_diffusion_mprm
run_id: r2_bon_phase_a
split: dev
run_ledger_path: orbit-research/RUN_LEDGER.jsonl

model:
  name: ace_step_v15
  backbone: ace_step
  sample_rate: 44100
  duration_seconds: 60.0
  cfg_default: 5.0
  inference_steps: 50
  latent_rate_factor: 64

reward:
  beta_robust: 0.5
  lambda_probe:
    silence_fraction: 0.0

baseline:
  rung_id: R2
  name: bon_raw
  description: BoN-{4,8,16} with raw primary reward; inference-time ceiling without robustness.
  seed: 0
  prompts_path: configs/prompts/dev.jsonl
  output_dir: runs/r2_bon
  bon_n: 16
  extras:
    n_sweep: [4, 8, 16]
    primary_axis: aesthetic_pq
    seeds: [0, 1, 2]
