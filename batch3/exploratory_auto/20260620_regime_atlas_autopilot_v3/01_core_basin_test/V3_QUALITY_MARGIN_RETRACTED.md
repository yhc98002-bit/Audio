# RETRACTED: V3_QUALITY_MARGIN.json is NOT apples-to-apples (Codex review 2026-06-23)
The worker scores only KEPT rows (seed_idx<4 OR detector-disagreement) → the bon_clean vs v3_clean
means are over biased subsets (BoN scored-clean dominated by instrumental/disagreement rows). The
-0.16 common_robust_lcb is likely a selection/stratum artifact, NOT a quality finding. Do NOT use.
To redo: score a fixed random sample of CLEAN draws per prompt, matched by stratum.
